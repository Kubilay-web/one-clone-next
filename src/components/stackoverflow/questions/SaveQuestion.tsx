import React from "react";

const SaveQuestion = ({
  questionId,
  hasSaved: initialHasSaved,
}: {
  questionId: string;
  hasSaved: boolean;
}) => {
  return <div>ddd</div>;
};

export default SaveQuestion;
